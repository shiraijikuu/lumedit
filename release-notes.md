# LumEdit v0.4.0

发布日期：2026-09-13

## 新增（色彩引擎深度对标 DaVinci Resolve）

- **半浮点渲染管线**：调色中间纹理升级为 RGBA16F 半浮点，多级调色串联不再累积 8bit 量化色带；启动时自动探测设备能力，不支持浮点渲染时安全回退
- **局部线性化**：曝光、白平衡、晕影等物理量在着色器内按线性光计算，+EV 曝光数值符合光学倍数，画质更纯正
- **3D LUT 四面体插值**：参考 OpenColorIO 的四面体插值替代三线性，色彩过渡更精确，大梯度处无网格伪色
- **专业示波器**：波形（Waveform）、矢量（Vectorscope）、RGB 排列（Parade）三种模式，调色有据可依
- **Soft Clip 软滚降**：高光 / 阴影两档平滑压缩，替代接近纯白纯黑时的生硬裁切
- **HSL 取色限定器（二级调色）**：按中心色相 + 范围 + 柔化圈选颜色，支持吸管点选，仅对选中颜色调曝光 / 色温 / 饱和
- **多局部蒙版叠加**：线性 / 径向蒙版最多叠加 8 个，可单选、复制、删除，直接在图上拖拽位置与范围
- **16bit PNG 导出**：全分辨率无损输出，零依赖 16 位/通道 PNG 编码器

## 修复

- 修复 RAW 图片打开「水印工作室」失败、RAW 竖图方向、应用水印后预览重建失败（v0.2.0 build 4）
- 修复局部渐变拖拽不跟手、线性渐变覆盖范围（改为 PS / darktable 语义：两端点定义过渡，垂直方向无限延伸）
- 含 v0.1.1 安全加固：IPC 文件读写授权、外部链接协议白名单、更新来源锁定、LUT 解析防 DoS

## 下载与安装

- 安装版：**LumEdit-0.4.0-setup.exe** 双击安装（Windows x64）
- 便携版：**LumEdit-0.4.0-portable.exe** 免安装单文件
- `latest.yml` 与 `.blockmap` 供应用内自动更新使用，无需手动下载。

纯本地处理，照片不上传。
