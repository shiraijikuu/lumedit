# 更新日志（CHANGELOG）

## v0.5.0（2026-09-14，build 7）

### 新增
- **一键自动优化**：基于 GPU 直方图分析 + 本地规则引擎，自动校正曝光、白平衡、对比度与饱和度；不引入 AI 模型，照片不出本机，分析目标 < 100ms，结果作为一次可撤销操作写入现有 EditParams.adjust
- **蒙版系统升级**：多蒙版组合语义（并集 / 交集 / 差集）、亮度范围蒙版、颜色范围蒙版、画笔蒙版；累积蒙版纹理确保局部调整按声明顺序正确合成
- **自动优化重置开关**：自动优化应用后显示状态开关，关闭时恢复自动优化前的影调参数
- **画笔蒙版交互补全**：局部蒙版新增画笔入口，开启绘制后可直接在画面拖动涂抹

### 修复
- 修复画笔蒙版输出整行全黑：补齐画笔程序的 uAspect uniform 定位，并在画笔光栅化后恢复全屏四边形 VAO；同时显式关闭 CULL_FACE，避免前后 pass 的 GL 状态泄漏
- 修复局部蒙版工具栏和线性 / 径向 / 亮度 / 颜色类型选择器文字溢出，按钮改为可换行自适应布局
- 修复局部蒙版拖拽命中区域：线性蒙版增加加宽透明拖拽线，径向蒙版内部可拖动，画笔模式切到其它蒙版时自动退出
- 修复矢量示波器目标色标记贴边溢出：增加绘制安全边距并钳制标签坐标
- 修复带画笔蒙版时的导出 Worker 克隆失败：cloneParams 深拷贝 strokes / pts，避免 Vue reactive Array Proxy 传给 postMessage
- 修复打包版水印工作室“预设图片水印”不可用：补齐内置品牌 PNG、动态 GIF 首帧预设与 cwm-logo 资源
- 修复 PNG16 导出上下倒置：统一按 WebGL 底部优先行序翻转为 PNG 顶起顺序
- 优化导出 / 复制速度：8bit 出口直接传 RGBA 原始像素，取消中间无损 PNG 编码再解码；PNG16 改用原生 CompressionStream deflate

### 测试
- 冒烟测试扩充到 188 项：画笔蒙版逐像素采样、自动优化规则与 GPU 直方图、画笔 strokes 深拷贝 Worker 克隆安全；分析耗时断言 < 100ms

## v0.4.0（2026-09-13，build 6）

### 新增（色彩引擎深度对标 DaVinci Resolve）
- **半浮点渲染管线**：调色中间纹理升级为 RGBA16F 半浮点，多级串联不再累积 8bit 量化色带；启动时能力探测、纹理池按格式分桶复用，设备不支持浮点渲染时安全回退 RGBA8
- **局部线性化**：曝光、白平衡、晕影、局部蒙版等物理量在着色器内 sRGB→linear 计算后再转回，+EV 曝光数值符合光学倍数（画质修正，不保留旧算法）
- **3D LUT 四面体插值**：参考 OpenColorIO 以四面体插值替代三线性，色彩过渡更精确、大梯度处无网格伪色
- **专业示波器**：波形（Waveform）、矢量（Vectorscope）、RGB 排列（Parade）三模式，Rec.601 色差映射与 R/M/B/C/G/Y 六目标色参考
- **Soft Clip 软滚降**：高光 / 阴影两档平滑压缩，替代接近纯白纯黑时的生硬裁切，阈值处 C1 连续、单调不反向
- **HSL 取色限定器（二级调色）**：按中心色相 + 范围 + 柔化圈选颜色，支持吸管点选，仅对选中颜色调曝光 / 色温 / 饱和
- **多局部蒙版叠加**：线性 / 径向蒙版最多叠加 8 个，可单选、复制、删除，画布直接拖拽位置与范围
- **16bit PNG 导出**：Worker 内浮点读回 + 零依赖 16 位/通道 PNG 编码器，全分辨率无损输出（不叠加水印、不缩放）

### 测试
- 冒烟测试扩充到 163 项：浮点中间纹理与读回、Soft Clip 直通/阈值/端点、取色限定隔离、多蒙版叠加与上限、示波器纯函数落点、PNG16 文件结构 / CRC / stored 分块解压往返、中英 i18n 对齐

## v0.3.0（2026-09-13，build 5）

### 新增
- **调色预设**：整套调色（影调/曲线/HSL/分级/效果/LUT 及强度）保存为命名预设，一键应用、删除管理，存于 userData 跨会话可用；应用走撤销栈可一键回退
- **白平衡吸管**：点取画面中性灰区域，按渲染公式反解并叠加色温/色调，Esc 取消
- **局部渐变**：线性/径向渐变滤镜，蒙版内应用曝光/色温/色调，位置/大小/旋转可调，随工程文件与撤销栈保存，预览与导出同源
- **分屏对比**：可拖动分割线左右对照原图与修改结果
- **剪裁警告**（快捷键 J）：高光溢出标红、阴影溢出标蓝的画面叠加蒙版
- **调整复制/粘贴**（Ctrl+Alt+C / Ctrl+Alt+V）：跨图片粘贴整套编辑参数
- **一键复制到剪贴板**（Ctrl+Shift+C）：修好的图以 PNG 写入系统剪贴板，直接粘贴使用
- **导出 LUT**：把当前调色（逐像素部分）经 GPU 烘焙为 17³ .cube 文件分享，烘焙与预览同管线零漂移
- **EXIF 查看面板**：顶栏「信息」查看相机/镜头/曝光参数/拍摄时间/GPS 等元数据
- **图片会话条**：一次打开多张图片，底部缩略图切换且各自保留编辑参数，上限 20 张
- **最近打开**：文件菜单保留最近 10 张图片；**会话恢复**：启动时自动恢复上次图片与全部编辑参数
- **导出长边快捷尺寸**：1080/2000px 一键换算并显示输出像素
- **裁剪参考线**：三分线/网格/黄金比例一键切换
- **便携版**：新增免安装单文件 LumEdit-x.y.z-portable.exe

### 调整
- 移除基础版双档构建路径，第二档功能并入标准版
- alert() 全部替换为非阻塞 toast 通知；批量处理拆分为独立 Pinia store
- 新增 GitHub Actions CI（push/PR 自动 typecheck + 冒烟测试）；发布脚本改为跨平台 Node 版（npm run publish）
- README 增加界面截图

### 修复
- **修复 dev 模式无法启动**：vite-plugin-electron 默认 lib.formats=['es'] 与用户配置 ['cjs'] 被 mergeConfig 数组拼接成双输出、同名写 main.cjs，不压缩构建时并发交错产生 SyntaxError；现按格式分流产物并从安装包排除 ES 副产物
- 批量处理取消可中断进行中的导出（含水印合成阶段）；进度与失败按条目索引上报，不同目录同名文件不再混淆
- 渲染管线中间纹理池化复用 + 参数变化 rAF 合帧，滑块拖动不再逐帧分配显存；修复换图时旧结果纹理泄漏

### 测试
- 冒烟测试扩充到 119 项：纹理池、LUT 烘焙 identity 往返与曝光响应、渐变 Stage 直通/蒙版内外行为等

## v0.2.0（2026-09-12，build 4）

### 新增
- **RAW 兼容**：支持索尼 ARW、DNG、NEF、CR2/CR3、RAF、ORF、RW2 等 20 种 RAW 扩展名。采用「提取 RAW 内嵌全尺寸 JPEG 预览」路线（不做 libraw 解拜耳），真实索尼 a7c2 ARW 验证可提取 7008×4672 全尺寸内嵌图；RAW 的拍摄元数据从 TIFF 容器抽取并在导出时回写
- **中英双语**：界面文案全量抽离到 i18n 词典，顶栏可一键切换 简体中文 / English 并持久化；原生菜单、系统对话框、更新提示同步双语；Chromium 语言包裁剪到 zh-CN / en-US（安装包体积下降约 7MB）
- **深化调色（对标 Lightroom / 达芬奇）**：
  - 第一档（基础版与完整版均含）：曝光、亮度、对比度、高光、阴影、白色、黑色、色温、色调、清晰度、去朦胧、饱和度、自然饱和，以及 RGB 主/红/绿/蓝四通道色调曲线（可加点、拖拽、双击删点）+ 实时直方图
  - 第二档（仅完整版）：8 色相 HSL 混色器（色相/饱和/明度）、阴影/中间调/高光颜色分级、晕影/颗粒/锐化/降噪效果
- **双版本发行**：同一套源码经编译期档位开关 `__APP_TIER__`（`LUMEDIT_TIER=basic/full`）产出「基础版 LumEdit Basic（仅第一档）」与「完整版 LumEdit（第一档+第二档）」，独立 appId/产品名/安装目录，可共存

### 修复（build 4）
- 修复 RAW 图片打开「水印工作室」失败：camera-watermark 无法直接解码 ARW / DNG / NEF 等 RAW 容器，现改为复用 LumEdit 已提取的内嵌预览与 EXIF，不再把 RAW 原始字节交给工作室解码
- 工作室初始化增加原图 ingest 失败回退，即使遇到其他浏览器不支持的原始格式，也能使用调色后预览正常进入编辑界面
- 修复 RAW 竖图导入后变横：RAW 内嵌 JPEG 缺失 Orientation 标签时，按 RAW TIFF 的 Orientation 手动转正
- 修复水印应用到图片后离屏预览重建失败：IPC 发送前将 Vue reactive 水印状态转换为普通对象，避免 “An object could not be cloned”

### 工程
- 渲染管线新增 Curve / HSL / ColorGrade / Effects 四个 WebGL2 Stage，预览与导出统一经 `createEditStageBundle()` 组装，保证所见即所得；中性参数全部直通短路
- 参数模型扩展为 geometry/adjust/curve/hsl/colorGrade/effects/lut(/watermark)，`ensureParams` 向前兼容旧工程与旧撤销快照
- 新增跨平台打包脚本 `scripts/dist.mjs`（`npm run dist:full` / `dist:basic`）

### 测试
- 冒烟测试扩充到 105 项：RAW 扩展名/签名识别、Orientation 8 转正、RAW EXIF → 水印工作室字段映射、合成 RAW 内嵌 JPEG 提取与 Electron 端到端预览解码、曲线 LUT 烘焙/插值、参数模型向前兼容与深拷贝、中英文词典 key 对齐、第二档 Stage 中性直通；`vue-tsc` 0 错误，105 项全部通过

## v0.1.1（2026-09-12）

### 安全加固
- **IPC 文件读写授权**：`fs:readBuffer` / `fs:writeFile` 不再接受任意路径。读取仅限本次会话中用户通过对话框选择的文件、以及打开的 `.lightedit` 工程引用的文件；写入仅限用户通过对话框选择的输出目录之内，杜绝渲染层被攻破后任意读写磁盘
- **外部链接协议白名单**：`shell:openExternal` 与窗口弹开行为仅允许 `http(s)` 链接，拒绝 `file://` 等任意协议
- **更新清单 URL 锁定**：远程 `update.json` 中的下载地址只信任本仓库 GitHub Releases（`shiraijikuu/lumedit`），其余一律回退到 Releases 页面；清单 `version` 字段增加格式校验
- **.cube 解析防 DoS**：`LUT_3D_SIZE` 上限收紧到 129（覆盖 17/33/65/129 全部常见规格）、解析文本上限 80MB、数据行先于 `LUT_3D_SIZE` 声明立即报错、超量数据点快速失败，恶意 LUT 文件不再能拖垮渲染层

### 修复
- 主进程 `webContents.on('crashed')` 改用现行 `render-process-gone` 事件
- `electron/` 目录纳入 `vue-tsc` 严格类型检查（此前从未被检查），并修复由此暴露的类型错误

### 测试
- 冒烟测试新增 19 项用例：文件访问授权策略（读/写/工程引用/路径逃逸）、更新清单 URL 白名单、cube 解析边界（声明顺序 / 尺寸上限 / 超量数据），共 60 项全部通过

## v0.1.0（2026-09-12）

- 首个版本：裁剪 / 旋转 / 翻转 / 固定比例；曝光、亮度、对比度、饱和度、色温；19 款内置 LUT + 外部 `.cube` + 「我的 LUT」库；camera-watermark 水印工作室；JPG / PNG / WebP 全分辨率保真导出（保留 EXIF、默认清除 GPS）；批量处理、`.lightedit` 工程文件、撤销/重做（50 步）、检查更新
